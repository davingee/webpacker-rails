import React from 'react'
import ReactDOM from 'react-dom'
import PropTypes from 'prop-types'

<% name = 'Erb' %>;

console.log('Hello world from <%= name %>')

const name = <%= name %>;

console.log('Hello world from <%= const %>')

export class Erb extends React.Component {
  
  const name = <%= name %>;
  
})